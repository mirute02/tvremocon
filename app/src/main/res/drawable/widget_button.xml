<?xml version="1.0" encoding="utf-8"?>
<!--
  Pressed state included so a tap looks like it registered immediately. The send itself is a
  network round trip and the status line only updates when it returns, which is too late to
  feel like feedback.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/widget_button_pressed" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/widget_button" />
            <corners android:radius="12dp" />
        </shape>
    </item>
</selector>
